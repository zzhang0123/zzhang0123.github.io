source 'https://rubygems.org'

gem 'github-pages', group: :jekyll_plugins
gem 'jekyll', '~> 3.8.5'
gem 'csv'  # Add this line to resolve CSV dependency
gem 'rubyzip', '~> 2.3.0'  # Lock Rubyzip to pre-3.0 version
gem 'bigdecimal'  # Add this line to resolve BigDecimal warning
gem 'rexml'
gem 'webrick'

group :jekyll_plugins do
  gem 'jekyll-paginate'
  gem 'jekyll-sitemap'
end